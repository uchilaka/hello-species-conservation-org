{
  "name": "@uche-hello-org/react-level-1",
  "version": "0.1.0",
  "scripts": {
    "test": "echo \"needs root level tests!\"",
    "start": "lerna run --stream --scope @uche-hello-org/ledger start",
    "start:cms": "lerna run --stream --scope @uche-hello-org/cms start"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "publishConfig": {
    "access": "restricted"
  }
}